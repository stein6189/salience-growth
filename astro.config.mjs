// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Change this to your final domain once registered.
  site: 'https://saliencegrowth.com',
});
